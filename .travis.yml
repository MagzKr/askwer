language: python
python:
    - 3.6
services:
    - docker

install:
    - pip install -r requirements.txt
before_script:
    -pip install docker-compose
    -docker-compose build
script:
    - docker-compose run web python askwer/tests.py
